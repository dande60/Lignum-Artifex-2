# Gallery Images

Drop portfolio photos into `assets/images/gallery/<category>/`.

Naming rules:
- Recommended: lowercase + hyphens (example: `cheese-board.jpg`)
- `cover.jpg` is the category cover and is ignored by the photo index
- `placeholder.png` and `.gitkeep` are ignored
